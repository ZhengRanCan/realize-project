===== check-map: experiments/framework-map-generation/fixture-a/run-03/framework-map.json =====
document      Pre-class Fusion Context Consumption 语义模型
granularity   section (provisional)   ⚠️ provisional — 不得与 sourceUnit 粒度混算
stats         elements 14 · edges 5 · attachments 8 · topics 7 · relationGap 3 · gapDensity 0.38
coverage      15/15 有路径（本粒度内）

HARD ERROR (0)
  （无）

WARNING (4)
  ! W1 element 总数 14 > preferred budget 12（认知容量 heuristic，非 semantic validity）；内容都有 L1/L2 入口 → 只需检查 Navigation invariant
  ! W5 relationGap[0] context-receipt ⇢ context-availability：「Availability 是 Receipt 之上的递进层级：上下文已到达（Receipt）是可用（Availability）的前提，但有 Receipt 不保证 Availability。」（REVIEW REQUIRED —— 现有词表无法在不失真前提下表达）
  ! W5 relationGap[1] context-availability ⇢ context-consumption：「Consumption 是 Availability 之上的递进层级：合法、冻结、版本一致的上下文已可用，不等于它被实际纳入本次课程设计。」（REVIEW REQUIRED —— 现有词表无法在不失真前提下表达）
  ! W5 relationGap[2] frozen-context ⇢ outline-generation-attempt：「二者的组合构成 Context Consumption 的 Subject：Frozen Context × Outline Generation Attempt。」（REVIEW REQUIRED —— 现有词表无法在不失真前提下表达）

INFORMATIONAL (4)
  i I1 component = 0（正常形态差异，不是缺陷）
  i I6 no-fourth-level 只挂了一个 block / section（单点 Topic，形态差异）
  i I6 consumption-subject 只挂了一个 block / section（单点 Topic，形态差异）
  i I6 primary-consumption-point 只挂了一个 block / section（单点 Topic，形态差异）

SKIPPED (0)
  （无 —— 本次全部检查都已执行）

════════════════════════════════
结果: HARD 0 · WARN 4 · INFO 4
状态: PASS
注: element budget / 单点 Topic / 某类元素为 0 / 无主轴 / DAG 是 Warning 或 Informational，不是语义缺陷。relationGap 少而散时逐条列 W5，多而密时聚合成一条 W8 + detail 段。
════════════════════════════════

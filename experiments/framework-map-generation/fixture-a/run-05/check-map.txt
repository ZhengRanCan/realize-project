===== check-map: experiments/framework-map-generation/fixture-a/run-05/framework-map.json =====
document      Pre-class Fusion Context Consumption 语义模型
granularity   section (provisional)   ⚠️ provisional — 不得与 sourceUnit 粒度混算
stats         elements 16 · edges 7 · attachments 9 · topics 7 · relationGap 2 · gapDensity 0.22
coverage      15/15 有路径（本粒度内）

HARD ERROR (0)
  （无）

WARNING (3)
  ! W1 element 总数 16 > preferred budget 12（认知容量 heuristic，非 semantic validity）；内容都有 L1/L2 入口 → 只需检查 Navigation invariant
  ! W5 relationGap[0] context_receipt ⇢ context_availability：「Availability 以 Receipt 为前提但不反向蕴含：上下文可以有 Receipt 而没有 Availability（digest 不一致、越权引用、冻结失败、旧 revision、切换 lesson session、浏览器伪造上下文等），两层不可互相替代。」（REVIEW REQUIRED —— 现有词表无法在不失真前提下表达）
  ! W5 relationGap[1] context_availability ⇢ context_consumption：「Consumption 以 Availability 为前提但不反向蕴含：合法冻结上下文已可用并不说明生成器真正使用了其中的教学语义。」（REVIEW REQUIRED —— 现有词表无法在不失真前提下表达）

INFORMATIONAL (4)
  i I1 component = 0（正常形态差异，不是缺陷）
  i I4 非单链拓扑：收敛节点 outline_attempt（按 consumes 归一化后的多入边）—— 形态差异，不是异常
  i I6 influence_excluded 只挂了一个 block / section（单点 Topic，形态差异）
  i I6 primary_point 只挂了一个 block / section（单点 Topic，形态差异）

SKIPPED (0)
  （无 —— 本次全部检查都已执行）

════════════════════════════════
结果: HARD 0 · WARN 3 · INFO 4
状态: PASS
注: element budget / 单点 Topic / 某类元素为 0 / 无主轴 / DAG 是 Warning 或 Informational，不是语义缺陷。relationGap 少而散时逐条列 W5，多而密时聚合成一条 W8 + detail 段。
════════════════════════════════
